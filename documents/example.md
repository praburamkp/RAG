# Example project

This example shows a local retrieval augmented generation workflow. Documents are split into overlapping chunks, scored against a question, and returned as context for a language model.
